# DataMining-Semantic-Miners
feature engineering outputs:
- TF-IDF features (train/test)
- BERT-ready text fields (train/test)
- Salary labels (low/medium/high)
- Enriched dataset with annual salary and combined text

traditional machine learning outputs:
- Random Forest, Naive Bayes, and Linear SVM trained on TF-IDF features
- Model evaluation using accuracy, precision, recall, and F1-score
- Confusion matrices generated for all three models
- Accuracy comparison across all the baseline models
